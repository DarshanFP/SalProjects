# Phase 0.1 — Applicant Role

## Status
- [x] Implemented
- [ ] Deployed
- [x] Verified (local)

## Reference
- Plan: Phase_Wise_Refactor_Plan.md → Phase 0 → 0.1
- Error: Production_Errors_Analysis_070226.md → Error 1

## Changes
- **Files modified**: `database/seeders/RolesAndPermissionsSeeder.php`
- **What changed**: Added `applicant` role via `Role::firstOrCreate(['name' => 'applicant', 'guard_name' => 'web'])`. Also updated existing roles to use `firstOrCreate` with `guard_name` for consistency and idempotency.

## Verification
- **Post-deploy**: Run `php artisan db:seed --class=RolesAndPermissionsSeeder` OR `php artisan tinker` → `Role::firstOrCreate(['name' => 'applicant', 'guard_name' => 'web']);`
- **DB check**: `SELECT * FROM roles WHERE name = 'applicant';` returns 1 row.
- **Smoke test**: Provincial/General creates new user with role "applicant" → no error; user appears in list.
- **Log scan**: Absence of `There is no role named applicant for guard web`
- **Rollback**: `DELETE FROM roles WHERE name = 'applicant';` only if no users have that role.

## Date
- Implemented: 2026-02-07
- Verified (local): 2026-02-08
